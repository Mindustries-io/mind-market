# Claude Code local/session files — never publish
.claude/
CLAUDE.local.md
**/settings.local.json
**/.claude/settings.local.json

# Local user data written by the plugins at runtime (lives in ~/.claude, but guard anyway)
**/config.json.bak
*.snapshot.json
discovery-log.json

# Secrets and environment
.env
.env.*
*.pem
*.key

# OS junk
Thumbs.db
Desktop.ini
.DS_Store

# Editors
.vscode/
.idea/
*.swp

# Scratch / build
node_modules/
dist/
tmp/
scratch/
